<% include partials/header %>

<div class="ui main text container segment">
<div class="ui huge header">Edit <%= tech.title %></div>

<form class="ui form" action="/tech/<%= tech._id %>?_method=PUT" method="POST">
    <div class="field">
            <label>Title</label>
    <input type="text" name="tech[title]" value="<%= tech.title %>">
    </div>
        <div class="field">
            <label>Image</label>
    <input type="text" name="tech[image]" value="<%= tech.image %>">
     </div>
        <div class="field">
            <label>Blog Content</label>
    <input type="text" name="tech[body]" value="<%= tech.body %>">
    </div>
    
    <button class="ui violet big basic button">Submit</button>
</form>
</div>
<% include partials/footer %>